{
  "DisableAll": true,
  "Enable": ["golint"],
  "Exclude": [
    "should have comment",
    "comment on exported",
    "should not use dot imports",
    "don't use ALL_CAPS in Go names; use CamelCase"
  ]
}
