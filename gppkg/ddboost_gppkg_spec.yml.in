Pkgname: ddboost
Architecture: x86_64
OS: ${OS}
Version: ${DDBOOST_VERSION}-gp${GPDB_MAJOR_VERSION}
GPDBVersion: ${GPDB_MAJOR_VERSION}
Description: ddboost plugin package
PostInstall:
- Master:  "echo 'ddboost plugin version ${DDBOOST_VERSION} successfully installed';"
